namespace MDPlus.Models
{
    /// <summary>
    /// Supported document formats in MDPlus.
    /// </summary>
    public enum DocumentFormat
    {
        Markdown,
        PlainText,
        Log,
        Csv,
        Tsv,
        Json,
        Ini,
        Cfg,
        Yaml,
        Xml
    }
}
